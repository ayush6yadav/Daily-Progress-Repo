##  Apache Configuration
* `sudo nano /etc/apache2/ports.conf`: Command to change the port Apache listens on.
* `sudo apache2ctl configtest`: **Crucial command** to check for syntax errors before restarting.
* `sudo systemctl restart apache2`: Apply changes.